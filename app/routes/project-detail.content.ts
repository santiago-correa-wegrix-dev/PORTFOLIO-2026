// oxlint-disable-next-line id-length
import { type Dictionary, t } from "intlayer";

const projectDetailContent: Dictionary = {
  content: {
    back: t({ en: "Back" }),
    challengeLabel: t({ en: "The Challenge" }),
    industryLabel: t({ en: "Industry" }),
    nextLabel: t({ en: "Next" }),
    prevLabel: t({ en: "Previous" }),
    roleLabel: t({ en: "Role" }),
    solutionLabel: t({ en: "The Solution" }),
    stackLabel: t({ en: "Tech Stack" }),
    visitSite: t({ en: "Visit Live Site" }),
    yearLabel: t({ en: "Year" }),
  },
  key: "project-detail",
};

export default projectDetailContent;
