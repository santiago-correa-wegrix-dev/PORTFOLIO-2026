import { type Dictionary, t } from "intlayer";

const writingContent: Dictionary = {
  content: {
    back: t({ en: "Back" }),
    minRead: t({ en: "min read" }),
    subtitle: t({ en: "Thoughts on engineering, AI, and building products" }),
    title: t({ en: "Writing" }),
  },
  key: "writing",
};

export default writingContent;
