// oxlint-disable-next-line id-length
import { type Dictionary, t } from "intlayer";

const projectsContent: Dictionary = {
  content: {
    title: t({ en: "Selected" }),
    titleAccent: t({ en: "Work" }),
    years: t({ en: "(2017 — 2026)" }),
  },
  key: "projects",
};

export default projectsContent;
